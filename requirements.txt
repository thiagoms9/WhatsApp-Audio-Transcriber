# Use faster-whisper instead of openai-whisper (much smaller and faster!)
faster-whisper>=0.9.0

# Minimal audio processing
pydub>=0.25.1

# Text processing
regex>=2023.10.3
python-dateutil>=2.8.2

# Web framework (for deployment)
fastapi>=0.104.1
uvicorn>=0.24.0
python-multipart>=0.0.6

# Utilities
tqdm>=4.66.1
loguru>=0.7.2

# Note: faster-whisper is much smaller than openai-whisper + torch
# It uses CPU inference and is specifically optimized for production